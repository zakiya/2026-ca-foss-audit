# web-tech-audit

## Documentation
- Whenever a change modifies the CLI's behavior, flags, output format/columns,
  or detection approach (`scripts/audit.py`, `scripts/signatures.py`), update
  the corresponding section(s) of `README.md` in the same change — Usage,
  Output, How detection works, and/or Limitations should always accurately
  reflect current CLI flags, output columns, and detection behavior.
